* text=auto eol=lf
*.docx binary
*.png binary
*.jpg binary
*.gif binary
*.pdf binary

# Batch files are parsed by cmd.exe, which mis-handles LF-only line endings.
# The window opens and nothing runs, with no error. Force CRLF.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
